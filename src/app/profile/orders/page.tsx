import OrderHistory from '@/components/OrderHistory';
import React from 'react';

const Page = () => {
    return (
        <div>
            <OrderHistory />
        </div>
    );
};

export default Page;